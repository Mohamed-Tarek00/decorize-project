import 'package:flutter/material.dart';

class WorkerHomeViewBody extends StatelessWidget {
  const WorkerHomeViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(children: [Text('Worker')]);
  }
}
